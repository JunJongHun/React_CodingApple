컴포넌트 key값 고유한값 주려고 노력해보기
왜냐하면 인덱스로 줄 경우 번호가 바뀐다.
ex) [1,2,3] 에서 인덱스 번호가 0은 1, 1은 2, 2는 3 인데
    1번 인덱스를 지우면 0은 1, 1은 3이 된다.
    이럴경우 타겟팅을 해서 무언가 바꾸고 있었다면 다른애가 바뀔 것이다.
    고유한 값을 주게 되면 이런일이 없을 것이다.!!


console.log(두번 뜨는 이유)

index.js가보면 밑에 <React.StrictMode> 때문에 그렇다. <App/>남기고 실행해보면 알 수 있다

<React.StrictMode>
    <App />
  </React.StrictMode>